# AGENTS.md

## Mission

Build an autonomous product storytelling studio that optimizes for audience retention, emotional engagement and factual credibility.

当前成片是严格小于 3 分钟的 9:16 竖屏视频。目标是提高陌生观众停止划动、继续
观看、理解产品并愿意讨论或转发的概率。

整体采用有事实支撑的正面推广口吻。优先让观众看懂产品能做什么、体验为什么有趣、
团队做了什么选择，以及用户如何使用。问题、风险、数据缺口和未来不确定性只在影响
产品理解、事实准确或发布合规时简短出现，不把它们做成全片主线，也不用未来质疑
收尾。正面推广不等于照念官网宣传语或替产品补写没有来源的效果。

每一期不以完整介绍产品为目标，而是回答一个具体问题：

“这个产品为什么在这个时间出现，团队做了什么选择，用户为什么开始接受它？”

事实决定可以说什么，观众注意力决定先说什么。研究材料不能直接变成旁白；只有能
推动核心问题、解释产品动作或改变观众判断的事实才能进入成片。

不承诺“爆款”或具体播放量。生产阶段使用前三秒理解速度、前二十秒悬念、每
20～40 秒的信息推进、可复述的核心发现和明确的转发理由作为质量代理指标。发布后
再用真实留存、完播、评论和转发数据验证。

## Editorial priority

发生冲突时，按以下顺序决策：

1. 事实不能被改写或虚构。
2. 优先呈现产品价值、用户体验和可验证的正面结果。
3. 全片只保留一个能被观众复述的故事问题。
4. 优先保留人物选择、用户动作、产品变化和可验证结果。
5. 信息完整度让位于理解速度和故事推进。
6. 技术、融资、平台和风险只有在影响用户体验、事实准确或发布合规时才进入旁白。
7. 来源说明优先放入画面小字和 Claim Ledger，不把研究过程念给观众。

## Short-video quality test

脚本进入制作前必须回答：

- 第一帧展示什么具体结果或动作？
- 陌生观众在三秒内能否看懂发生了什么？
- 二十秒结束时，观众等待的答案是什么？
- 第一处可能划走的位置在哪里？
- 每个段落新增了什么，而不是重复解释什么？
- 哪个发现值得观众复述给别人？
- 观众会把视频转发给谁，理由是什么？
- 删除哪些资料后，故事反而更清楚？
- 结尾是否兑现开场，没有转去总结功能或强行升华？

无法回答以上问题时，脚本不得进入 TTS 和渲染。

## Current delivery profile

- 当前只交付 1080×1920、30 fps 的 9:16 竖版视频。
- 最终 MP4 必须严格小于 180 秒。以真实 TTS 和最终 MP4 的读回时长为准，不以脚本
  目标时长、smoke render 或四舍五入后的“3 分钟”代替。
- 不渲染横版视频，不用横版通过代替竖版检查。
- 当前先解决文案自然度。只有口播改写、口播评审、观众评审和事实评审全部通过，
  才允许进入 TTS。
- 旁白默认使用 `config/tts-v2.json` 选择的中文 TTS provider。当前默认 MiniMax，
  API Key 只从 `MINIMAX_API_KEY` 环境变量读取；缺少凭据或服务失败时，按配置回退
  到项目 `.venv` 中的 Microsoft Edge neural TTS。
- provider、音色、语速、音高和回退策略只进配置文件。旧 Edge 配置保留
  `zh-CN-YunjianNeural`、rate `+25%`、pitch `-2Hz`。
- 项目永久排除自部署 GPT、LLM 和语音生成模型；不建设 GPU 推理服务，也不把
  GPT-SoVITS、CosyVoice 等自托管方案保留为候选或 fallback。
- 音频生成后用 FFmpeg 归一化到 `I=-16`、`TP=-1.5`、`LRA=7`。
- 每次改旁白都要重新生成分段音频、测量真实时长、重建字幕和时间轴。
- 自动化 `polish` 与初稿生成分开。system prompt、风格规则、禁用词和 few-shot
  样本目录都从配置加载；每次运行必须落逐轮 judge 评分报告。

## Facts first

- 分开核对首次出现、正式开放、增长事件和指标统计期。
- 每句事实旁白都要引用 Claim Ledger 中存在且允许播出的 claim ID。
- 公司披露、创始人口述、媒体报道和编辑判断必须在画面或旁白中说清楚。
- 消息数不能换算成用户数、留存、收入、人均强度或成功任务数。
- 不猜创始人动机、后台架构、增长归因、市场验证或素材权利。
- 外部素材必须登记在 asset manifest。权利不清楚时，改用代码图形或直接删掉。
- 产品有公开官网、应用商店页面或官方应用截图，且画面能解释当前主线时，每期默认
  至少使用一处真实官方产品画面；官网和应用内截图都可用时，优先各保留一处。不能
  为了凑截图加入与故事问题无关的页面。
- 官方演示视频、发布视频或可追溯的真实操作录屏能直接证明核心动作时，按真实产品
  画面管理：登记来源、权利依据、用途和 Claim ID，并优先在强事实首次出现时使用。
- 截图必须来自可追溯的官方 URL。捕获日期、来源 URL、权利主体、编辑使用依据、
  使用目的和对应 claim ID 必须写入 `production/asset-manifest.json`。渲染画面要
  标明“真实页面截图”和具体来源，不把官网宣传语直接当作独立事实。
- 真实页面不存在、页面无法稳定访问或发布权利不清楚时，改用有 Claim 支持的
  程序化图形；合成界面必须标“功能演示”，不得伪装成真实应用截图。

## Script development workflow

- 先完成 research、sources 和 Claim Ledger，再搭故事。资料还不能支持转折时，调整
  结构，不用推测补齐。
- 写大纲前，先用一句话写出全片要回答的问题，再分别写出目前有来源的答案和不能
  回答的边界。
- `Story Director` 必须把核心问题、观众承诺、有来源答案、事实边界、情绪弧线和
  信息揭示顺序写入 `director-brief.md`。该文件绑定当前 facts、sources 和 research
  timeline 的 SHA-256，是后续注意力策略和故事结构的导演决策源。
- 为开场准备多个候选。比较前三秒吸引力、信息密度、悬念、可信度和画面表现力，
  选择能靠事实和画面成立的一版，不选只靠夸张措辞的一版。
- Hook 候选必须分别写出首帧已经发生的结果、与旁白同期出现的证据、0～3 秒、
  3～10 秒和 10～20 秒的信息递进，以及观众最自然的误解。首帧不能只有动作开端；
  零背景观众应能看见一个已经改变的状态。
- 开场第一句不得以陌生公司名或产品名加归因起头。第一段不超过 3 秒，必须先给出
  零背景观众能看懂的具体动作。统计口径可以在动作之后 5 至 10 秒补充，它是加分
  项，不是 Hook 淘汰门槛。
- 前 20 秒内必须用一句普通话建立产品心智模型，让零背景观众知道它是什么、在哪里
  使用或替用户完成什么。定义必须受 Claim 支持，不能为了简洁写出“没有 App”等
  未核实的排他性结论。
- 先让观众看见具体使用场景、愿望或麻烦，再讲产品历史。不要为了制造冲突把正常
  使用写成危机。每个新功能、协议或专有名词出现前，必须先交代它接住了哪个已出现
  的需求；接不住主线的概念直接删除。
- 除非用户明确要求直接写完整稿，先交付带时间轴的 "视频结构大纲" 和
  "脚本亮点规划"，等待确认后再写完整分镜和口播。
- 大纲的每一段必须写明时间范围、叙事任务、可见动作或证据画面、claim ID、来源
  身份、节奏切换和不能越过的事实边界。没有对应 claim 的事实不能进入口播计划。
- 强事实第一次进入旁白时，对应的实拍、真实页面、来源标签或 Claim 支持的图形必须
  同期出现，不能先靠旁白建立印象，再在后段补交证据。
- 每段还要写清它相对上一段增加了哪个产品价值、用户动作、尺度、选择、后果或判断
  变化。信息没有增量时，压缩或删除；不能用换画风、音效或重复数字冒充推进。
- 故事必须能连成“产品要解决或证明什么 → 团队做了什么选择 → 用户看见或做了
  什么”。精彩事件无法接回产品选择时，只能作为素材候选，不能成为主线。
- 资料支持时，优先使用“先看结果 → 解释产品选择或机制 → 回到同一结果”的结构，
  让重复画面获得新的含义。它不是强制三段模板，不能为了套结构虚构幕后代价。
- 每期最多安排一处有来源支持的认知修正，纠正观众基于画面自然产生的误解。没有
  可验证误解时不制造“原来如此”或反转。
- 大纲必须为可用的官网或应用截图指定进入时间、叙事作用和对应 claim。截图优先
  用于前 20 秒建立产品心智模型、首次解释核心动作，或后续展示真实产品状态；不能
  只作为无信息量的装饰背景。
- "脚本亮点规划" 只列真正能维持注意力的节点，并说明它靠哪条新信息、哪个动作
  或哪份证据成立。梗图、BGM 停顿和夸张标题不能代替内容。
- `Viral Director` 在写稿前把选中的注意力设计写入 `viral-strategy.md`。它必须评估
  Opening hook、Curiosity gap、Emotional tension、Information reveal order 和
  Ending payoff，并绑定当前故事输入的 SHA-256。策略只能重新排列有 Claim 支持的
  信息，不能制造情绪、因果或播放量承诺。
- `Script Writer` 先把信息正确、Claim 完整的版本写入 `script-draft.md`，不得直接
  把初稿标成最终口播。
- 独立的 `Oral Rewriter` 再把初稿改成 `final-script.md`。它只能改变中文表达和
  句间节奏，不能新增事实、人物、场景、因果或 Claim。
- `final-script.md` 必须能直接拆进 `script.json`。每段至少包含 section、
  narration、onScreenText、claimIds、scene、visualIntent 和 targetSeconds。
- 独立的 `Oral Judge` 必须在 `oral-review.md` 中绑定初稿和最终稿的 SHA-256，
  分别评估中文自然度、口播节奏和信息保真。三项均不低于 4/5 且没有 blocker 才能
  继续；最多回改 3 轮，第三轮仍不通过就交给人工编辑，不无限自动重写。
- Audience Critic 与 Fact Guardian 通过后，`Visual Director` 必须在
  `visual-plan.md` 中为每个旁白段落定义 scene structure、visual evidence、animation
  ideas、asset requirements、pacing 和完整 Claim IDs。计划绑定最终稿 SHA-256；关键
  素材未取得、权利不清或竖屏不可读时不得标记 READY。
- 独立的 `Retention Critic` 必须在 `retention-report.md` 中绑定最终稿与视觉方案的
  SHA-256，预测前三秒、前三十秒、中段和结尾的划走风险。总分不低于 80/100、每项
  不低于 15/25、没有 high risk 或 blocker 才能进入 TTS。它只指定退回角色，不直接
  改稿或改视觉方案。
- Audience Critic 和 Retention Critic 的划走风险必须写清时间、严重度、观众为什么
  会走、当前证据、要求发生的具体变化和责任角色。不得只写“节奏弱”或“建议优化”。
- Critic REJECT 后，`workflow.json` 必须记录 feedback ID、owner 和待修改产物。下一轮
  评审必须引用上一轮报告，逐条记录修改前后产物的 SHA-256；产物没有实际变化时不得
  把 REJECT 改成 PASS。创意修订最多三轮，仍失败时交给人工编辑。
- 用“因为、所以、于是、结果”连接两个事件时，Claim 必须支持因果。资料只支持先后
  顺序时，用具体动作呈现变化，不靠连接词制造因果。
- 不把 "百万播放量" 当作质量结论。只能检查开场速度、信息推进、证据可信度、
  画面变化和结尾边界这些可观察项。

## Text style: de-ai

写旁白、标题、画面文案和发布文案时，先做一遍去 AI 味检查。初稿的信息整理和
最终口播改写是两个独立阶段，不允许一次生成后直接跳到评审。

### 禁止

- 禁止使用 "总而言之"、"不可否认"、"在某某浪潮中"、"让我们拭目以待"。
- 禁止用 "这不仅是，更是"、"既有，又有"、"不仅，而且" 做工整收束。
- 禁止用 "不是……而是……"、"并非……而是……" 制造反转或强行下结论。
- 禁止在结尾升华主题，也禁止用“以后还能不能成功”一类问题压住正面介绍。最后
  一句停在有 Claim 支持的具体事实、产品状态或用户动作上。
- 禁止堆 "沉浸式"、"极致"、"史诗级"、"震撼"、"完美融合" 等广告词。
- 禁止连续使用 "第一层、第二层、第三层" 或三句长度相同的排比。
- 禁止把 "点火事件"、"增长引擎"、"价值闭环"、"生态位" 之类抽象词当解释。
  如果必须使用，紧接着写出具体动作和可观察结果。
- 禁止制造金句式短句串联。短句可以出现，但不能每句都像片尾海报。
- 禁止用破折号制造停顿。改用句号、逗号或直接重写。
- 禁止把研究过程直接念给观众。旁白不说 "公开资料没有披露"、"能确认的只有"
  或 "资料到这里断了"。没有数据时，换成观众听得懂的具体边界，或者删掉。
- 禁止沿用英文的主语、长定语和动宾顺序逐句翻译。出现“把 A、B 和 C 装进 D”
  一类生硬结构时，先改成一个人会怎样问、怎样做、怎样得到结果。
- 禁止把“于是、随后、结果”当通用转场。只有语义真的成立时才保留。

### 写法

- 先写摄像机和屏幕能看见的动作。例如：复制邮件里的日期、点开日历、补一条
  提醒。不要只写 "任务在工具间流转"。
- 用普通词。能写 "打开" 就不写 "激活入口"，能写 "改日历" 就不写
  "执行跨系统动作"。
- 长短句要错开。允许口语停顿、自我校正和不完全对称的节奏。
- 默认一句只说一个意思，尽量控制在 25 个汉字左右。超过时优先拆开长定语、并列项
  和从句；不能为了达标把一句话切成连续的海报短句。
- 口播要有明确对象感。可以自然地问一句、补一句或改口，但不能机械插入反问、
  “说人话”或口头禅来伪装真人。
- 保留不确定性，但不要堆免责声明。说一次来源边界，然后继续故事。
- 先消化英文来源，再按中文口语重新组织。不要沿用英文原句的语序、抽象名词和
  从句结构，也不要逐句翻译研究档案。
- 公司口径、媒体口径和资料来源优先放在画面小字中。只有来源身份会改变观众对
  事实的判断时，旁白才点一次出处。
- 全片旁白显式来源归因最多 2 次。来源边界、数据口径和指标缺口只进画面小字，
  旁白不说“这个口径没有拆分”“看不出”“回答不了”。
- 不写“听上去很技术”“说白了”一类元评论。技术词首次出现必须紧跟普通话解释，
  否则删掉。
- Poke、Recipe、Cognition 等专有名词保留原文；第一次出现时必须用一句普通话
  说明它是什么或做什么。
- 标点就是 TTS 的停顿提示。数字、年代和英文缩写按实际读法检查，不能只保证屏幕
  上看得懂。
- 具体细节必须来自来源或明确标注为演示。不能为了显得真实而编造人物、房间、
  身体反应或生活用品。
- 每段旁白朗读一遍。删掉播音时需要换气两次才能说完的句子。
- 结尾回到开头的问题，用具体动作回答，停在可验证的产品价值、用户动作或正面结果。
- 结尾不得追加“你觉得怎么样”“评论区告诉我”“点赞关注”“加入俱乐部”等通用
  互动 CTA。发布平台需要的引导放发布文案，不占用故事结尾。

## Style assets

- `style/voice-guide.md` 是稳定风格规则；`style/approved/` 只放人工明确认可、可以
  复用的历史成稿。
- Oral Rewriter 每轮最多选 2～3 份与当前题材最接近的 approved 样稿作为参考，并
  在 `oral-review.md` 记录文件路径。没有 approved 样稿时，只使用 voice guide，
  不把 AI 自己写的稿件倒灌成 few-shot。
- 风格样稿只能影响说法，不能成为事实来源。样稿和当前 Claim 冲突时，以当前
  Claim Ledger 为准。

## Caption style

- `content/<episode>/story/caption-plan.json` 是人工可审计的字幕语义规划。旁白不变时，
  先在同一 cue 内用最多两行保留完整语义单元，再按真实音频重新计算 cue 时间。
- 字幕规划拼接后必须与旁白逐字一致；不得为了排版删词、换词或补词。
- 不把主语、谓语、修饰语、产品名或来源动词的内容拆到相邻 cue。需要换行时，优先
  在同一 cue 内换行，而不是让观众等待下一条字幕。
- 每条字幕是一个独立显示片段。片段末尾的逗号、句号、问号、感叹号、分号和冒号
  全部删除，片段内部的标点保留。
- 字幕切分后再检查末尾标点，不能依赖旁白原文手工处理。
- 字幕必须与真实音频重新对齐，不能只改 SRT 文本。

## Video prompt style

只有在确实需要生成式视频素材时，才写视频 Prompt。UI 讲解、网页证据和数据镜头
优先使用真实截图或程序化图形。

Prompt 按以下顺序写：

1. Subject and action：主体正在做什么，包含一个小而不完全对称的动作。
2. Camera：机位、景别、焦段、轨迹、速度和停止点。
3. Light and texture：具体光源、表面材质和真实瑕疵。
4. Motion：惯性、延迟、24 fps 运动模糊或手持漂移等可观察现象。

视频 Prompt 禁用以下词：

`cinematic`, `photorealistic`, `masterpiece`, `ultra-detailed`, `4k`, `8k`,
`epic`, `breathtaking`, `stunning`, `hyperrealistic`, `unreal engine`

把抽象描述换成可执行摄影指令：

- "电影感" 改成具体胶片、焦段、景深和机位。
- "震撼光影" 改成光从哪里来、穿过什么介质、落在哪个表面。
- "精致面孔" 改成毛孔、细绒毛、雀斑和不对称表情。
- "流畅动作" 改成相机抖动、运动模糊、加速和减速曲线。

不要机械套用 35mm、浅景深或体积光。镜头不需要这些，就不要写。

## Story shape

- 前 3 秒必须给出零背景可懂的具体动作。前 20 秒补出反常识动作和一个与观众
  使用、信任或后果有关的问题；统计口径只作可选加分项。
- 可以从结果发生前的一刻切入，也可以提出有分歧的问题，但开场中的人物、动作、
  数字和争议都必须有来源或明确标注为演示。
- 故事按四段推进，但不要把四段写成研究报告标题：
  1. 先给结果和问题，让观众知道接下来要弄清什么。
  2. 回到具体困难，说明人物做了什么，产品接着怎么改。
  3. 给出用户动作和已证实的结果，继续展示产品价值、传播方式或必要的权限设计。
  4. 回答开场问题，停在一个有 Claim 支持的具体事实、产品状态或用户动作上。
- 不强制制造危机和反转。资料只能证明时间相邻时，就写时间顺序，不补成因果。
- 默认不把缺点、风险、争议、付费压力或未来不确定性串成主线。只有它们直接改变
  用户体验、事实判断或发布合规时才简短说明，随后回到产品本身。
- 不做终极升华，不把产品变化写成时代趋势，也不替公开资料宣布市场胜负。
- 全片围绕一个能复述的故事问题推进。不要把 Idea、验证、机制、增长和风险写成
  研究报告目录。
- 每一段都要承接上一段发生的动作。人物做了什么、产品因此怎么改、用户接着如何
  使用，优先于编辑分析和方法论说明。
- 全片最多讲三个产品动作，但不要用同样句式编号。
- 用具体动作区分时间先后和因果。事件挨得近，不代表前者造成后者。
- 技术只讲会改变用户体验、权限、成本或分发的部分。
- 中段不得写成日期编年。日期只有在改变观众判断时才进入旁白；与用户体验无关的
  监管或平台条款只进画面小字或直接删除。
- 每 20 至 40 秒检查一次是否出现新动作、新证据或问题推进。长时间没有推进时，
  压缩旁白或重排段落。
- 每 60 至 90 秒至少安排一次明显的节奏切换。可以切到真实截图、时间线、程序化
  图形、新问题或短暂静音。切换必须承接故事，不能为了刷新画面硬插梗图。
- BGM 停顿、画风变化和音效只能强调已经发生的信息变化，不能制造资料没有支持的
  紧张、胜利或反转。
- 结尾不能替公开资料回答留存、盈利或增长归因，也不能用这些未知项反问产品未来。

## Engineering

- 使用 TypeScript strict mode、pnpm、React、Remotion、Zod、Vitest 和 FFmpeg。
- episode 内容放在 `content/<episode>/`，复用代码放在 `src/`。
- research、director brief、viral strategy、script draft、final script、oral review、
  visual plan、retention report、workflow、timeline、comparison report 和 manifests
  是 source of truth。
- 不手修最终 MP4。更新内容或组件后重渲。
- 当前仓库只保留 9:16 产品故事生产链，不注册或生成旧项目与横版 Composition。
- 角色化写作通过 Codex Prompt 和文件交接完成，不在仓库内调用 LLM API，不增加
  LangGraph、AutoGen、CrewAI 或自部署模型运行时。

## Delivery review

- `oral-pass` 只代表口播自然度和信息保真通过；还要经过 Audience Critic、Fact
  Guardian、Visual Director 与 Retention Critic。只有注意力策略 READY、视觉方案
  READY、留存评审 PASS 且 `pnpm validate:story` 通过，状态才是 `story-approved`。
- `story-approved` 只批准脚本，不代表成片合格。TTS、字幕、时间轴和竖版渲染完成
  后，必须由独立 `Delivery Critic` 审核实际 MP4、SRT 和真实 TTS 时长。
- Delivery Critic 的硬 blocker：英文单词或中文词组在词中断开；小于 1.0 秒的
  cue 占比超过 10%；第一屏对零背景观众不可懂；真实语速导致吞字或字幕错位；
  最终 MP4 时长达到或超过 180 秒。
- Delivery Critic 必须抽查所有官网和应用截图镜头，确认关键界面在竖屏中可读、
  来源标签可见、字幕没有遮住证据重点，并与 asset manifest 的 URL、用途和 claim
  一致。无法辨认或缺少来源标签时退回 `render`。
- Delivery Critic 还必须抽查所有真实视频和操作录屏的首次出现，确认核心动作在
  9:16 中可辨认、强事实与证据同期、来源标签可见，且没有用长期模糊填充代替有效
  构图。任一项不成立时退回 `render`。
- 任一 blocker 命中即退回 `captions`、`timeline`、`tts` 或 `render`。Delivery
  Critic 不直接改产物，也不拿脚本阅读体验代替成片审核。
- Delivery Critic 报告必须绑定当前竖版 MP4、SRT 与 production timeline 的
  SHA-256。只有 `pnpm validate:delivery` 通过，状态才是 `delivery-approved`。
- 复刻既有 episode 时，保留旧 MP4 与 timeline，并用 `comparison-report.md` 绑定
  两版哈希。Hook、故事连贯性、观众好奇心、产品理解、视觉叙事和留存潜力分别评分；
  这些是编辑代理指标，不能代替发布后的真实留存或 A/B 数据。

## Required checks

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm validate:research
pnpm validate:workflow
pnpm validate:story
pnpm materialize:story
pnpm validate:content
pnpm tts
pnpm timeline
pnpm render:smoke
pnpm render:vertical
pnpm inspect:output
pnpm validate:delivery
pnpm validate:comparison
```

只有竖版读回、字幕安全区、来源谱系、音频和真实时长都通过，才能说本轮完成。
`validate:comparison` 只在本轮包含既有 episode 复刻对比时要求执行。
