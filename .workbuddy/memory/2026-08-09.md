# 2026-08-09

## 全面代码审查（已交付 reports/code-review-2026-08-09.md）

- 实测基线：tsc 0 错、eslint 0 警、vitest 119/119 通过（仅 Node 24；Node 22 下 better-sqlite3 ABI 不匹配挂 4 个 checkpoint 测试）。
- 严重发现 5 项：render.ts composition 选择对新 episode 静默渲染旧集成片；fadeScene interpolate 短场景输入范围非单调；TTS/LLM fetch 无超时无重试；content-subgraph/freeze/TTS 回退/validate 脚本测试盲区；RoostEpisode 内容嗅探+下标分发场景。
- 关键结构问题：渲染层 as 断言绕过 zod schema；v1/v2 双轨死配置（tts/polish 各一套且 bannedTerms 已漂移）；禁用写法规则三处真源；单集特化（seg-id 留白、品牌名正则、抓取 URL）渗入通用脚本。
- 环境注意：本机沙箱 shell 无 pnpm，需用 node_modules/.bin 直跑；测试须用 Node 24（/Users/zengze/.nvm/versions/node/v24.13.0/bin）。
